// For now, It'll just log the error
// Later we'll implement the logic of the the logging

export const logService = {
  init,
  log
};

function init() {
}

function log(error) {
  //console.error(error);
}
